[package]
name = "voop-protocol"
version = "0.1.0"
edition = "2021"

[features]
std = []
uniffi = ["dep:uniffi", "std"]

[dependencies]
uniffi = { version = "0.29", optional = true }

[lib]
crate-type = ["lib"]
