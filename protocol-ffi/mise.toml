[tasks.build]
description = "Compile voop-protocol-ffi: macOS cdylib (for bindgen) and iOS staticlibs (for xcframework)"
sources = ["src/**/*.rs", "Cargo.toml", "../protocol/src/**/*.rs", "../protocol/Cargo.toml"]
outputs = [
  "../target/release/libvoop_protocol.dylib",
  "../target/aarch64-apple-ios/release/libvoop_protocol.a",
  "../target/aarch64-apple-ios-sim/release/libvoop_protocol.a",
]
run = """
cargo build -p voop-protocol-ffi --release
cargo build -p voop-protocol-ffi --release --target aarch64-apple-ios
cargo build -p voop-protocol-ffi --release --target aarch64-apple-ios-sim
"""
